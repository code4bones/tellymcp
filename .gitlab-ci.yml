stages:
  - deploy

deploy:gateway:
  stage: deploy
  tags:
    - france
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v?\d+\.\d+\.\d+$/'
  variables:
    GIT_STRATEGY: fetch
  script:
    - bash scripts/deploy-gateway.sh
