FROM node:24-bookworm-slim

WORKDIR /app

ENV NODE_ENV=production
ARG TELLYMCP_VERSION=0.0.10

RUN npm install -g "@deadragdoll/tellymcp@${TELLYMCP_VERSION}"

EXPOSE 8080

CMD ["tellymcp", "run", "--env", "/app/.env-gateway"]
